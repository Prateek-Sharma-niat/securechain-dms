{
  "name": "securechain_dms",
  "version": "1.0.0",
  "description": "SecureChain DMS - Fullstack Tamper-Evident Legal Record & WORM Repository",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
